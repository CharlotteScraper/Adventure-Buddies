import UIKit
import Flutter

class GeneratedPluginRegistrant: NSObject {
  static func register(with registry: FlutterPluginRegistry) {
    // Plugins are registered automatically via Flutter's plugin mechanism
  }
}